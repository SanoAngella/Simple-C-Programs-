#include <stdio.h>

int main() {
    int lower, upper, i, j, flag;

    printf("Enter two numbers (range): ");
    scanf("%d %d", &lower, &upper);

    for (i = lower + 1; i < upper; ++i) {
        if (i <= 1) continue;
        
        flag = 0;

        for (j = 2; j <= i / 2; ++j) {
            if (i % j == 0) {
                flag = 1;
                break;
            }
        }

        if (flag == 0) {
            printf("%d ", i);
        }
    }

    return 0;
}
